[supervisor]
port = 8080
log_level = 'warn'

[planner]
base_folder = 'import'

[[planner.batches]]
name = 'schema'
folders = ['schema']

[[planner.batches]]
name = 'data'
folders = ['schema', 'data']

[[planner.batches]]
name = 'performance'
folders = ['schema', 'data', 'perf']
